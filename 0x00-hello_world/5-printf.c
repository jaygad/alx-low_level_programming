#include <stdio.h>

/**
 * main - Entry point
 * use the function printf
 * compile with -Wall gcc without warning
 */

int main(void)

{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
